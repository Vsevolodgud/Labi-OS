First program (runs only one) progOne.c writes to shared memory and second program (runs in one or more instances) 
progTwo.c reads them and prints to the console.
In addition to shared memory, semaphore used here to control the threads. 
